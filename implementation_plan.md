# 让玩家在 Matter 详情页里"看见一件事正在被推动"

本次设计与实现分为 5 趟，前 4 趟为产出理解与设计，第 5 趟为落地实现。请确认前 4 趟的推演与第 5 趟的实施方案，确认后我将开始着手修改代码。

## 趟 1：理解 Matter 在游戏里的真正位置（产物 A）

**1. Matter 和"任务"、"事件"、"动作"分别有什么本质不同？**
- **动作 (Action)**：是操作的最小单元，是玩家“点下去”的那一下（如：回微信、拨打电话、点发送）。
- **事件 (Event)**：是动作发生后留下的客观事实日志（如：14:00 发送了带看邀约，14:05 客户拒绝）。
- **任务 (Task/Quest)**：是静态的待办条目，通常是“做完即消”的检查清单。
- **Matter**：是**“正在推进的一件具体事”**，它拥有独立的生命周期、持续的上下文、往复的拉扯过程（由多个动作和事件组成），并最终影响游戏里的关系（Relation）和运行态（Runtime）。

**2. 玩家推进一条 Matter 的过程，最像现实里的什么经历？**
最像现实中**“在微信里跟某人/某群持续跟进一件事”**，或者**“处理一个带审批和流转的工单（Ticket/Case）”**。
这个类比决定的 UI 隐喻：**它不该是一个静态的表单或属性面板，而应该是带有时间线、有上下文对话流、有“对方正在输入/对方卡点”的【案卷（Case File）】或【沟通流（Thread）】。**

**3. 四类 Matter 在节奏、信息密度、玩家投入感上的差异**
- **report（汇报）**：节奏“单向抛出->等待回应”，信息密度“偏事实/证据展示”，投入感“中等，像递交报告等批示”。
- **diagnose（诊断）**：节奏“极快（自我分析）”，信息密度“极高（同维数据对比/指标盘）”，投入感“偏向纯策略推理，像看体检报告”。
- **execute（执行）**：节奏“重型、阶段清晰（准备->发车->结算）”，信息密度“中等（聚焦结果漏斗）”，投入感“极高，消耗核心资源（精力/推广金），且部分为实时专屏”。
- **negotiate（博弈）**：节奏“来回拉扯（试探->锚定->交换->收口）”，信息密度“极度聚焦（只盯核心分歧点，如价格/意愿）”，投入感“极深，像打牌走钢丝，一旦破裂影响巨大”。

---

## 趟 2：设计四种"形状"（产物 B）

为避免四类 Matter 共用模板，每一类必须拥有符合其业务性质的专属“形状”。

### 1. Report (汇报型) - 形状：回执单与对话流 (Receipt & Thread)
- **视觉骨架**：类似邮件链或微信聊天流。左侧/上方是玩家准备的“事实包”，右侧/下方是对方的“回执反馈”。
- **推进时的反馈信号**：点击发送后，出现类似“对方正在看报表”的等待动画（沙漏或...），然后气泡浮现“业主已读，并表示...”。
- **历史回溯方式**：向下滚动的对话流，明确记录“周二你发了什么数据，对方当时怎么回的”。
- **失败时的呈现**：不会凭空消失，而是流的末尾盖上红色的 `[沟通未果]`，保留对方拒绝的最后一句原话（如“这个价格我不认”）。

```ascii
[汇报：星辉海岸 带看反馈]
=============================
(你) 昨天 14:00
> 发送了 [本周 3 组带看客户画像]
> “李姐，这三组看完都觉得客厅采光有遮挡...”
-----------------------------
(李姐) 昨天 14:30
< “我觉得是他们没看懂，这可是东向！”
=============================
[下一步动作] -> (继续安抚) (拿出竞品对比)
```

### 2. Diagnose (诊断型) - 形状：X光机与对比卡 (X-Ray Dashboard)
- **视觉骨架**：数据对比卡与仪表盘。左边是当前房源数据，右边是市场大盘/竞品基准。
- **推进时的反馈信号**：玩家点击“收集线索”或“分析”时，对应的指标条像扫码一样点亮（绿代表健康，红代表卡点）。
- **历史回溯方式**：保留生成的“诊断快照（Snapshot）”，玩家可翻阅“上周诊断出的问题是价格虚高”。
- **失败时的呈现**：诊断本身不失败，但可能显示 `[线索不足，无法得出有效结论]`，保留灰色的空缺模块，提醒玩家先去做外部动作。

```ascii
[诊断：为何连续一周无带看]
=============================
[当前热度: 15] <-----> [商圈均值: 45] !!警报
[价格站位: 940W] <---> [近期成交: 910W] !!卡点
-----------------------------
诊断结论：价格脱离核心客群承受带。
建议动作：-> (开启一轮业主降价沟通)
```

### 3. Execute (执行型) - 形状：行动打卡与进度漏斗 (Action Tracker & Funnel)
- **视觉骨架**：清晰的阶段流程节点（准备 -> 发车 -> 结算）与漏斗数据。
- **推进时的反馈信号**：消耗资源（精力/金钱）时，对应的池子有扣减动效。节点被点亮打勾，进度条往前推。
- **历史回溯方式**：类似物流追踪，记录 `10:00 推广上线 -> 14:00 获得 50 个曝光 -> 18:00 带来 2 组带看`。
- **失败时的呈现**：流程停滞在中间节点，标红并显示失败原因（如“精力不足”或“业主临时取消配合”），进度条定格。

```ascii
[执行：私域周末主推] 消耗: -2精力 -500推广金
=============================
(✓) 1. 确认主推说辞
(✓) 2. 扣除资源，开始推送
( ) 3. 承接转化 (进行中...)
=============================
当前效果：20 曝光 -> 3 咨询
[手动结束并结算]
```

### 4. Negotiate (博弈型) - 形状：筹码桌与拉锯绳 (Chip Table & Tug-of-War)
- **视觉骨架**：双方阵营对立的视觉。中间是一条代表“共识度”的拉锯绳，两侧展示双方当前的“底线预期”。
- **推进时的反馈信号**：每次出价或试探，拉锯绳中间的指示器会滑动。如果试探触碰底线，会出现强烈的震动反馈和屏幕泛红警示。
- **历史回溯方式**：筹码桌旁边有一叠“历史出价单”，点开能看“第1轮：客出900万，主咬950万”。
- **失败时的呈现**：拉锯绳断裂，或者桌面上盖上 `[谈判破裂]` 的大印。记录下崩盘的那一个动作。

```ascii
[博弈：940万底价拉锯]
业主预期 [950万] =======X==== [930万] 客户意向
=============================
> 你尝试抛出: "客户最多加 5 万，首付多给 1 成"
< 业主反馈: "不行，950少一分免谈。" (愤怒值↑)
-----------------------------
[下一步] -> (继续压价) (转让部分佣金) (搁置)
```

---

## 趟 3：跨 Matter 视图（产物 C）

**1. 聚合视图怎么呈现"哪条 Matter 今天最该被推一下"？**
- 增加**“呼吸红点”**或**“燃烧/倒计时边框”**。
- 按状态排序：将“My Turn（需我处理）”高亮置顶，“Their Turn（等待对方）”置灰挂起。带有超时风险的 Matter 加上 `[限时剩余 2h]` 标签。

**2. 资源冲突的感知怎么做？**
- **预扣闪烁 (Hover Preview)**：当玩家鼠标悬浮在某个推进行动上时，右上角的“精力池”或“推广金池”会高亮显示对应的扣减额（比如 `-2 闪烁`）。如果余额不足，数字变红，行动按钮灰显并抖动。

**3. “卡点不在你身上”的呈现**
- 明确剥离玩家视线压力。如果一条 Matter 卡在业主环节，UI 上这卡片会被折叠或变淡，右上角出现明显的 **[等待业主回复 ⏳]** 标签或沙漏图标，不再抢夺视觉焦点。

---

## 趟 4：失败模式预演（产物 D）

| 场景 | 错误做法 | 正确做法 |
|---|---|---|
| **1. 汇报遭拒** | 一条 Matter 推到第三步后被业主拒绝，UI 直接把它标成"失败"并灰掉或消失。 | 将状态设为 `[已封存-破裂]`，保留最后一步拒绝的红字气泡，允许玩家回溯查看，并作为下次沟通的上下文依据。 |
| **2. 并发瘫痪** | 玩家同时有 6 条待推进 Matter，全部堆在一起只按时间排序。 | 区分“我的回合/必做”、“推荐做”和“等待中”。强引导当前最具破局点的那一条。 |
| **3. 资源耗尽无反馈** | 玩家精力不足点击推进，按钮无反应或只弹出全局 Toast。 | 按钮不可点，点击时发生剧烈抖动，并原地在卡片上浮现 `[需 2 点精力，当前不足]` 字样。 |
| **4. 超时暴毙** | Matter 很久没推直接判定失败，玩家懵逼。 | 提前 24 小时卡片边缘开始出现“冻结蔓延”或“红色倒数”，提示 `[即将错失时机]`。 |
| **5. 专屏遮挡** | 进入“开放日专屏”时，错过外面的重要 Matter。 | 专屏具有排他性，但右下角会有低打扰的“信件飞入”，仅提示 `[有新的紧急 Matter 插入]`，不强制中断。 |
| **6. 诊断死胡同** | 诊断 Matter 推一半发现缺关键事实，直接标失败。 | 卡在“需要进一步信息”节点，提示玩家返回房源/市场页寻找线索后再回来继续。 |
| **7. 谈崩** | 业主暴怒导致博弈直接关闭窗口。 | 画面震动，直接盖上红章 `[谈判破裂]`，窗口保留但不可再操作，让玩家看到最后崩盘的惨状。 |
| **8. 忘点发送** | 玩家在 Report 里面填了数据却没点发送就切出去了。 | Report 停留在 `[草稿]` 状态，并不真正产生世界影响，卡片上显示铅笔图标提示未发出。 |

---

## 趟 5：实现计划（产物 E）

为了落地这些设计，以下是架构和 React 组件修改方案：

### 核心约束
1. **绝不 if-else 复用组件**：我们将创建四个独立的 React Component：
   - `ReportMatterView.tsx`
   - `DiagnoseMatterView.tsx`
   - `ExecuteMatterView.tsx`
   - `NegotiateMatterView.tsx`
2. **局部反馈不用全局 Toast**：所有的错误提示、资源扣减提示都会在各自的组件内部以 inline error/tooltip 的方式呈现。
3. **回看上下文**：Matter 的 state 渲染不仅取当前的 `actionIds`，还要读取 `eventIds` 并在组件内将其还原为一条事件流（时间线/聊天记录）。

### 实施步骤
1. **重构当前 Matter 渲染逻辑**：目前 `SellingHousesWorkspace.tsx` 和 `Cases.tsx` 中可能只用了一个通用的 `ActionDecisionOverlay`。我们需要基于 Matter 的 `lifecycleCategory`（report/diagnose/execute/negotiate）来动态分发到那四个新设计的专属组件上。
2. **新建四个 Shape 的 UI 组件**（根据产物 B 的设计图写具体的 Tailwind + Lucide 布局）。
3. **实现交互与动画**：
   - 为 Negotiate 加入“底线拉锯”样式。
   - 为 Report 加入聊天流样式。
   - 实现悬浮预扣资源效果。
4. **验证与走查**：Selfplay 测试，确保能肉眼感知这是一件被推动的事。

> [!IMPORTANT]
> **需要您的确认**：以上针对 Matter 的理解、四种 Shape 的 UI 隐喻设计、并发视角的梳理以及落地实现拆分是否符合您的期望？确认后，我将立刻展开代码层面的新建和修改！
