# 开放日测算 - 设计规范

## 布局结构

### 整体流程
- **两页分离**：上传页 → 测算工作台，用户体验清晰
- **步骤拆分**：第一步只做数据准备，第二步集中处理参数和结果，避免信息过载

### 上传页结构
| 区域 | 宽度 | 作用 |
|------|------|------|
| 上传卡片 | 主列 | 上传按钮 + 数据统计 + 字段映射 + 下一步 |
| 最近测算 | 侧列 | 展示最近 4 条历史记录 |

### 测算页结构
| 区域 | 位置 | 作用 |
|------|------|------|
| 头部 | 顶部 | 返回按钮 + 数据集信息 + 更换文件按钮 |
| 左侧边栏 | 左上 | 字段映射 + 策略选择 + 参数调整 |
| 主内容 | 右侧 | 结果概览 → 自动解读 → Top 6 图表 → 完整表格 → 历史测算 |
| 结果流向 | 从上到下 | 参数 → 解读 → 图表 → 表格，符合用户思考顺序 |

## 颜色方案

| 角色 | 色值 | 用途 |
|------|------|------|
| 背景渐变 | `#f8f5f1` → `#f2ede6` | 页面背景 |
| 卡片背景 | `rgba(255, 255, 255, 0.85)` | 区块容器 |
| 主按钮 | `#bf623b` | 下一步、重新测算 |
| 次级按钮 | `rgba(255, 255, 255, 0.88)` | 加载示例、下载示例 |
| 成功文字 | `#255648` | 成功提示 |
| 错误文字 | `#a8422e` | 错误提示 |
| 标题文字 | `#1f1b17` | 标题 |
| 辅助文字 | `#6b6156` | 描述、标签 |
| 边框 | `rgba(76, 58, 42, 0.12)` | 输入框、卡片边框 |

## 圆角

| 尺寸 | 使用场景 |
|------|----------|
| 999px | 按钮、标签、进度条 |
| 14px | 表格、统计卡片 |
| 16px | 输入框、小卡片 |
| 24px | 区块面板 |

## 间距

| 尺寸 | 使用场景 |
|------|----------|
| 4px | 标签文字间距 |
| 8px | 相邻元素间距 |
| 12px | 网格间距 |
| 16px | 区块内间距 |
| 20px | 网格行距 |
| 24px | 区块间距 |
| 24px | 页面左右padding |
| 80px | 页面底部padding（确保表格完整可见） |

## 字体

| 字号 | 行高 | 用途 |
|------|------|------|
| 20px | 1.2 | 区块标题 |
| 16px | 1.5 | 按钮文字 |
| 14px | 1.5 | 正文、选项 |
| 13px | 1.5 | 标签、辅助文字 |
| 12px | 1.5 | 小标题、说明 |
| 11px | 1.5 | 迷你标签 |

**字重**：
- 标题：700 (bold)
- 按钮：700
- 标签：400
- 正文：400

## 断点（响应式）

| 断点 | 自适应变化 |
|------|----------|
| `< 1080px` | 网格变为 1fr 单列布局 |
| `>= 1080px` | 保持多列设计 |

## 交互

| 元素 | 动效 |
|------|------|
| 按钮、卡片 | hover 向上 1px，微浮效果 |
| 状态变化 | 160ms transition |
| 页面滚动 | 原生 overflow-y: auto |

## 设计原则

1. **内容优先**：去掉所有不必要的说明文案，只保留必要操作
2. **分步认知**：数据准备和参数调整分开，用户一次只处理一件事
3. **流向自然**：参数 → 解读 → 图表 → 表格，从上到下符合思考顺序
4. **足够留白**：底部保留 80px 间距，确保表格完整可见，不会被裁切
5. **干净无图标**：区块标题去掉图标，避免挤压文字，保持清爽
6. **卡片式分组**：相关参数放在一个卡片，视觉清晰

## 组件清单

### 上传页
- `open-day-upload-card` - 上传主卡片
- `open-day-upload-actions` - 操作按钮组
- `open-day-mapping-grid` - 字段映射网格（2 列）
- `open-day-upload-stats` - 数据统计信息

### 测算页
- `open-day-panel` - 通用区块面板
- `open-day-panel__head` - 区块标题区
- `open-day-preset-grid` - 策略选择卡片网格（2 列）
- `open-day-params-grid` - 参数输入网格（2 列）
- `open-day-waterline-grid` - 水位线网格（4 列桌面端）
- `open-day-waterline-card` - 单个水位线卡片
- `open-day-analysis-grid` - 自动解读网格（4 列）
- `open-day-analysis-card` - 单个解读卡片
- `open-day-chart` - Top 6 条形图
- `open-day-table` - 完整排名表格
- `open-day-history-grid` - 历史测算网格
