# 开放日测算 - 设计系统

## 项目级 UI 调性

这是一个统一入口下的多业务工作台，不是单一页面产品。整体调性应保持：

- **干净克制**：白底、轻阴影、大圆角、低噪音边框，不做重品牌化堆叠。
- **业务工作台**：以数据、状态、动作、反馈为核心，不用营销页叙事方式组织。
- **模块分色，不分裂风格**：不同模块可以有自己的强调色，但底层卡片、排版、交互密度应属于同一套系统。
- **先结构，后装饰**：优先保证信息分层、扫读效率、操作路径，再谈视觉强调。

## 模块密度约束

这个项目不是所有模块都用同一种信息密度。密度是业务属性，不是用户开关。

### 1. 多模型 PK

- 调性：轻量、现代、偏展示型工作台。
- 密度：中等偏舒展。
- 原因：主要任务是输入、选择、阅读、对比，不需要持续压榨同屏信息。

### 2. 小区开放日选址

- 调性：专业测算台。
- 密度：中等。
- 原因：核心是上传、参数配置、结果判断，强调流程清晰和认知负荷低。
- 约束：上传阶段应更舒展；分析阶段可以略微收紧，但不能牺牲表格可读性。

### 3. 我是王牌资产顾问

- 调性：经营驾驶舱 / 业务模拟台。
- 密度：**默认高信息密度**。
- 原因：同屏需要同时承载房源、线索、风险、节奏、资源和动作，主界面必须更紧凑。
- 这是**编码约束**，不是设置项，不需要暴露给用户选择。

## 紧凑不等于缩小

“紧凑”不能简单理解为把字号、边距、卡片全部变小。对经营游戏而言，正确做法是：

- **同屏信息更多，但层级更清楚**
- **默认扫读更快，但关键详情仍可展开**
- **主界面紧凑，浮层/弹框保持完整阅读体验**

禁止的做法：

- 把所有区域一刀切缩小
- 为了紧凑牺牲点击热区
- 为了塞更多内容而打乱主次
- 让弹框也跟着被压成难读的密集卡片

## 经营游戏的信息架构

“我是王牌资产顾问”后续所有 UI 迭代，都优先遵守下面这套信息架构。

### 一、主界面优先级

经营游戏主界面应围绕三类核心问题组织：

1. **我现在在经营什么**
2. **这些对象现在处于什么状态**
3. **我下一步能做什么**

因此主界面内容优先级应是：

1. 经营对象
2. 状态信号
3. 可执行动作
4. 结果反馈
5. 历史沉淀

### 二、推荐分区

#### A. 顶部全局状态

用于回答“今天是什么局面”：

- 当前天数 / 周节奏
- 预算、精力、资源位
- 快捷推进动作
- 当前阶段标签

要求：

- 一屏抬头就能看到
- 信息短、状态化、不可塞太多说明文字

#### B. 中央主决策区

用于回答“我现在最该盯哪个盘、做什么动作”：

- 当前房源 / 当前案例
- 本周目标
- 主风险
- 关键判断
- 可执行动作列表

要求：

- 是主阅读区，视觉权重最高
- 内容要按“判断 -> 动作”组织，不按“组件种类”堆砌

#### C. 辅助状态区

用于承载经营背景：

- 准客池
- 风险监控
- 市场异动
- 周节奏 / 日历

要求：

- 为主决策提供背景，不抢主区焦点
- 更适合卡片组、状态条、紧凑列表

#### D. 回溯与复盘区

用于回答“过去发生了什么”：

- 日志
- 周报
- 经营归因
- 复盘建议

要求：

- 是次级区，不要压过实时经营
- 可以长，但默认不该占据最高视觉优先级

## 经营游戏的交互模型

### 主工作区

主工作区适合：

- 快速切换对象
- 观察状态
- 做出动作
- 看即时反馈

因此主工作区应偏：

- 较小但清楚的卡片
- 更短的标题和标签
- 更紧凑的列表
- 更强的栅格对齐

### 浮层 / 弹框 / Overlay

浮层适合：

- 阅读完整总结
- 做带上下文的选择
- 承接阶段性反馈
- 完成复盘或结算

因此浮层应偏：

- 更完整的留白
- 更稳定的阅读节奏
- 更大的段落呼吸
- 不与主界面争同一种“高密度”

结论：

- **主工作区紧凑**
- **弹框不必紧凑**
- **弹框优先可读、沉浸、确认感**

## 经营游戏看板重构建议

这一节专门回答三个问题：

1. 当前首页看板里哪些信息不该放在首页
2. 未来日期是否真的需要展示那么多
3. 难度信息应该出现在哪一层

### 一、当前看板里不该放首页中心位的内容

以下内容不是“没用”，而是**不适合占据首页高优先级区域**。

#### 1. 世界版本 / World v / 世界层说明

这类信息属于：

- 研发设定
- 存档稳定性说明
- 世界观元信息

用户在经营时真正关心的是：

- 我今天资源多少
- 哪个盘最危险
- 下一步先做什么

因此：

- `World vX`
- “随机流和 scenarioSnapshot 一起写进存档”
- “当前月份受世界季节曲线影响”

这些都不适合占首页主区。

建议落点：

- 剧本详情页
- 帮助说明
- 次级信息抽屉

#### 2. “本局教学重点”整块

“本局教学重点”这个概念本身有价值，但现在的承载方式过于研发解释型，不够玩家决策型。

当前问题：

- 更像系统解释，不像经营建议
- 包含世界层 / 剧本层 / 运行层三层概念，对首页太重
- 用户看到后未必知道“所以我现在该做什么”

建议改成：

- **本局目标**
- **本局风险**
- **本局建议打法**

也就是从“解释系统”改成“指导经营”。

#### 3. 当前剧本里的统计信息过多

首页顶部如果同时出现：

- 房源数量
- 竞争组数量
- 脚本事件数量
- 剧本主题
- 剧本描述
- 难度
- 时限

会把注意力从“当前经营局势”拉回到“开局元数据”。

其中真正适合首页保留的只有：

- 剧本名
- 本局目标 / 主题
- 剩余天数

其余内容更适合：

- 剧本详情页
- 开局页
- 暂停页 / 信息页

### 二、哪些内容可以视为当前“首页噪音”

首页噪音的判断标准不是“是否有数据”，而是：

- 是否影响当前决策
- 是否需要高频查看
- 是否必须和其他信息一起看

基于这个标准，当前首页里以下内容应降级：

- 世界版本号
- 脚本事件总数
- 竞争组数量
- “运行层”解释
- 面向系统机制的教学说明
- 对未来 7 天逐日展开的完整预演

### 三、未来日期不需要那么多信息

结论：**不需要。**

原因不是界面塞不下，而是游戏里未来信息本身不是确定信息。

未来天数里真正稳定的，只有：

- 周几
- 节奏主题
- 基础精力
- 固定例行事项

未来天数里不稳定或不该过度承诺的有：

- 当天重点
- 实际优先事项
- 风险排序
- 真实事件流
- 市场异动

这些都应该随着当天状态变化而变化。

所以首页不该给用户一种错觉：

- “未来 7 天都已经被系统排好了”

这会削弱经营游戏的应变感，也会让玩家误以为未来是确定脚本而不是动态盘面。

### 四、未来日期的正确展示方式

首页只需要展示两层未来信息：

#### 1. 今天

今天要完整展示：

- 今日资源
- 今日主目标
- 今日优先事项
- 今日风险
- 今日建议动作

#### 2. 未来 1 到 2 天

未来只展示“轻预告”：

- 周几 / 主题
- 基础精力
- 固定例行事项

不要展示：

- 完整重点列表
- 完整事件流
- 完整优先级
- 看似确定的行动建议

#### 3. 更远日期

更远日期只需要做成：

- 一条周节奏条
- 或一组轻量 chips
- 或“本周节奏概览”

作用是告诉用户：

- 这个礼拜什么时候高能
- 什么时候是房源聚焦日
- 什么时候适合带看 / 反馈 / 获客

而不是告诉用户：

- 第 5 天你会具体做什么

### 五、难度不应该放在首页中心位

结论：**不应该。**

原因：

- 难度是开局选择，不是过程决策
- 一旦进入经营，用户更关心“当前压力”而不是“难度标签”
- “hard / standard / easy”对实时判断帮助很弱

首页中心位应该让给：

- 当前经营目标
- 当前主风险
- 当前最值得处理的对象

### 六、难度信息的正确位置

#### 一级位置：开局页

难度最适合在 [ScenarioSetup.tsx](/Users/jiaqi/Documents/开放日测算/src/selling-houses/ui/features/ScenarioSetup.tsx) 里作为主决策。

原因：

- 这里是用户真正做“训练强度选择”的地方
- 用户此时需要对比不同难度的节奏和压力

#### 二级位置：运行中的弱标签

进入游戏后，难度只需要保留成一个低权重状态标签，例如：

- 顶部栏的小 badge
- 剧本名旁的弱标签
- 暂停面板中的元信息

它的作用应该是：

- 帮用户记住当前在打什么强度

而不是：

- 占一个首页主卡

#### 三级位置：复盘页 / 结算页

难度在复盘和结算页仍然有价值，因为它可以解释：

- 这局的分数含金量
- 当前结果相对什么强度产生

所以适合保留在：

- 周报页
- 结算页
- 历史战绩

### 七、建议中的首页结构

经营游戏首页建议收敛为 4 个区，而不是现在这种“元信息 + 教学解释 + 周历 + 今日 + 事件流 + 侧栏”的并列堆放。

#### A. 顶栏：全局状态

只放：

- 剧本名
- 当前天数 / 剩余天数
- 预算 / 精力
- 难度弱标签
- 推进操作

不放长说明。

#### B. 主区：今天该怎么打

只回答：

- 今日主题
- 当前主目标
- 当前主风险
- 建议先做什么

这是首页真正的核心。

#### C. 辅助区：局势背景

放：

- 市场异动
- 风险监控
- 优先事项
- 轻量周节奏

其中周节奏应缩成“未来概览”，不再展开成完整未来看板。

#### D. 回溯区：历史与复盘

放：

- 经营日志
- 周报
- 经营归因

默认靠下，不与今日决策争主位。

### 八、首页应保留 / 下放 / 移位清单

#### 首页应保留

- 当前天数 / 剩余天数
- 当前资源
- 今日主题
- 当前主目标
- 当前主风险
- 建议优先事项
- 市场异动
- 风险监控
- 历史日志入口

#### 首页应下放

- 世界版本
- 脚本事件总数
- 竞争组数量
- 运行机制解释
- 系统教学层级说明

#### 首页应移位但保留

- 难度：移到顶部弱标签 / 结算页 / 复盘页
- 剧本描述：移到剧本详情或暂停页
- 未来日历：缩成未来 1-2 天 + 周节奏概览

### 九、实现约束

后续如果继续改经营游戏首页，遵守下面这些硬规则：

1. 首页主区只服务“当前决策”，不服务世界观解释。
2. 未来信息只能展示稳定节奏，不能伪装成确定安排。
3. 难度不能占首页中心卡位。
4. 元信息可以存在，但必须降级到弱标签、二级页或复盘页。
5. 首页每新增一块信息，都要回答：
   这个信息是否直接帮助玩家决定“现在先做什么”？

## 房源详情页设计

房源详情页不是“信息展示页”，而是经营游戏里的**单资产决策页**。

它的核心任务只有三个：

1. 让我快速看懂这套房现在是什么局面
2. 让我立刻知道接下来能做什么动作
3. 让我在需要时再下钻查看详细信息

因此页面结构固定为三段：

1. **总览区**
2. **动作区**
3. **信息区（Tab）**

---

### 一、页面结构

推荐结构如下：

```text
┌──────────────────────────────────────────────────────┐
│ 房源总览                                             │
│ 标题 / 阶段 / 聚焦标签 / 价格 / 主目标 / 主风险       │
│ 核心指标条：信任 / 热度 / 紧迫度 / 竞争力 / 窗口      │
├──────────────────────────────────────────────────────┤
│ 可做的动作区                                         │
│ 动作按钮卡片：业主沟通 / 精修卖点 / 调价 / 带看 / ... │
│ 每个动作都带“适合现在做吗”与成本提示                 │
├──────────────────────────────────────────────────────┤
│ 信息区（Tab）                                        │
│ [经营判断] [业主] [价格与好房分] [准客池] [日志归因]  │
│                                                      │
│ 当前 Tab 内容                                         │
└──────────────────────────────────────────────────────┘
```

这个顺序不能反：

- 先看局面
- 再做动作
- 最后才进入细节

---

### 二、总览区

总览区是房源详情页里最重要的部分，要求用户 3 秒内能回答：

- 这套房现在推进到哪一步了
- 当前最值得解决的问题是什么
- 这套房值不值得我现在投入资源

#### 1. 总览区必须包含

- 房源标题
- 当前阶段
- 是否为本周聚焦
- 挂牌价 / 市场心理价
- 今日主目标
- 当前主风险
- 建议打法一句话

#### 2. 指标层必须短

总览区下方的指标建议统一成短指标，不要塞成长解释。

建议保留：

- 信任度
- 热度
- 紧迫度
- 好房分
- 剩余窗口天数

这些指标适合做成：

- 横向小卡
- 小型进度条
- 数值 + 简短标签

不适合放在总览区的内容：

- 长段落房源故事
- 详细归因解释
- 准客池分布细表
- 历史日志

#### 3. 总览区的文案风格

总览区文案应该偏判断，不偏描述。

应该写：

- `业主信任偏脆，先稳关系再谈调价`
- `价格站位略硬，但盘面热度还撑得住`
- `窗口正在收缩，这套房不能再拖`

不应该写：

- `当前业主比较着急，最近状态有一些波动`
- `市场上也有一些竞品存在，需要进一步关注`

总览要像经营判断，不像工作汇报。

---

### 三、动作区

动作区要紧跟总览区，因为房源详情页是做决策的地方，不是单纯看资料的地方。

#### 1. 动作区目标

动作区要回答：

- 这套房现在能做哪些动作
- 哪些动作适合现在做
- 每个动作的成本和收益方向是什么

#### 2. 动作卡结构

每个动作卡建议包含：

- 动作名
- 一句话说明
- 成本
- 当前适配度
- 执行按钮

推荐结构：

```text
[调价沟通]
在价格、速度和关系之间做取舍
成本：1 精力 / 0 现金
当前适配：高
[执行]
```

#### 3. 动作区的排序规则

动作不能按固定枚举顺序摆放，应该按当前房源局面排序。

优先级建议：

1. 当前最推荐的动作
2. 当前可做但收益一般的动作
3. 当前不建议做的动作

表现方式：

- 推荐动作放最前
- 可做动作中性展示
- 不建议动作弱化或禁用

#### 4. 动作区不该承载什么

动作区不应该塞：

- 复杂背景说明
- 长策略解释
- 业主画像全文
- 准客池详情

这些内容应该进入 Tab。

---

### 四、信息区必须使用 Tab

信息区的作用不是让用户“一次看完全部”，而是让用户按问题进入不同维度。

因此信息区必须做成 Tab，而不是长页面顺序堆叠。

原因：

- 业主信息、价格信息、准客信息、历史归因是不同思维任务
- 如果全堆在一个页面里，阅读成本高，决策路径会断
- Tab 能把“当前动作决策”和“需要时下钻”分开

---

### 五、Tab 设计

推荐使用 5 个 Tab。

#### Tab 1：经营判断

这是默认 Tab。

它是“总览区”的展开版，用来回答：

- 这套房为什么值得做 / 不值得做
- 这套房当前最该解决什么
- 现在最应该先做哪类动作

建议内容：

- 本周目标
- 当前主风险
- 优势 / 短板 / 最该修
- 建议打法摘要
- 当前动作建议排序

这是最应该和动作区联动的 Tab。

#### Tab 2：业主

回答：

- 业主现在是什么状态
- 业主的配合风险在哪里
- 应该怎么沟通

建议内容：

- 业主当前状态
- 业主画像
- 信任 / 耐心 / 紧迫度
- 沟通建议
- 最近一次影响业主关系的关键事件

这个 Tab 是“关系判断页”。

#### Tab 3：价格与好房分

回答：

- 现在这套房在市场里的站位如何
- 价格是不是主要问题
- 盘面强弱来自哪里

建议内容：

- 挂牌价 / 市场价 / 底价
- 当前价格站位判断
- 好房分
- D1 / D2 / D3
- 强项与短板
- 价格动作历史

这个 Tab 是“盘面判断页”。

#### Tab 4：准客池

回答：

- 这套房当前的机会池厚不厚
- 还处在预测阶段的人群有多少
- 已经进入接洽推进的人有多少
- 最值得推进的是谁

这个 Tab 不是一堆“线索卡”的列表，而应该固定拆成两层：

1. `预测客群`
2. `接洽中的客户`

其中：

- `预测客群` 表示画像上可能匹配，但需求和成交力度还没确认的人
- `接洽中的客户` 表示已经接上话、可以继续推进的人

建议内容：

- 活跃准客数量
- 高意向数量
- 预测客群数量
- 接洽中的客户数量
- 阶段分布
- 准客列表

这个 Tab 是“漏斗判断页”。

#### Tab 5：日志归因

回答：

- 这套房为什么变成今天这个样子
- 最近几天到底发生了什么
- 哪些动作有效，哪些动作无效

建议内容：

- 经营归因
- 最近关键事件
- 竞争力变化
- 近期动作记录

这个 Tab 是“回溯页”。

---

### 六、Tab 的默认规则

#### 默认打开

默认应打开：`经营判断`

原因：

- 用户进入房源详情页，第一需求通常不是读资料
- 而是判断“这套房现在该怎么打”

#### 切换规则

切换 Tab 不应影响：

- 顶部总览
- 动作区

也就是说：

- 总览与动作区始终固定在上方
- Tab 只控制下方信息区内容

#### 视觉规则

Tab 不要做得太重，不要像一级导航。

应该表现为：

- 单行 tabs
- 小号 uppercase / 中等字重
- 选中态清楚，但不抢总览区

---

### 七、信息层级原则

房源详情页必须遵守：

#### 第一层：判断

在总览区给结论。

#### 第二层：动作

在动作区给操作。

#### 第三层：证据

在 Tab 区给依据。

不要把证据层提前到判断层前面。

错误顺序：

- 一上来先给一堆数据
- 用户自己猜该做什么

正确顺序：

- 先告诉用户这套房现在是什么局
- 再告诉用户能做什么
- 最后给用户细节和依据

---

### 八、房源详情页不应该做成什么

不要做成：

- 资料档案页
- 指标大全页
- 长滚动说明页
- 多段卡片平铺页

更不应该做成：

- “业主区一块、好房分区一块、准客区一块、日志区一块”全部顺序堆在同一页

因为这样用户每次都要重新扫整页，动作决策效率会很低。

---

### 九、实现约束

后续如果重做房源详情页，遵守以下规则：

1. 最上方必须是总览，不是详情。
2. 动作区必须位于总览之后，且不进入 Tab。
3. 信息区必须做成 Tab，不做长页面堆叠。
4. 默认 Tab 必须是“经营判断”。
5. Tab 内容的目标是支撑决策，不是补充面面俱到的资料。


## 经营游戏的卡片规则

### 适合做成小卡片的内容

- 数值指标
- 单条预警
- 单条优先事项
- 阶段分布
- 简短建议

特点：

- 单卡只表达一个点
- 一眼可扫完
- 不承担复杂阅读

### 适合做成中卡片的内容

- 业主状态
- 好房分
- 当日卡片
- 市场异动摘要

特点：

- 允许 2 到 4 个相关信息组合出现
- 适合“标题 + 说明 + 一组状态”

### 适合进入弹框或整页的内容

- 每日总结
- 经营结算
- 决策分支选择
- 周报复盘

特点：

- 包含上下文
- 需要连续阅读
- 需要用户停下来理解

## 空间与层级原则

### 舒展模块

适用于多模型 PK、开放日上传阶段：

- 更大段落留白
- 更少同屏决策点
- 更适合引导和解释

### 紧凑模块

适用于经营游戏主工作区：

- 更小但稳定的卡片间距
- 更少的装饰性留白
- 更强的网格对齐
- 更多的状态标签和摘要信息

注意：

- 紧凑模块要压的是**冗余留白**
- 不能压掉**结构间距**
- 不同层级之间仍然要有明确呼吸

## 当前设计文档的适用方式

这个文档分两层使用：

1. **项目级规则**
2. **模块级规则**

如果两者冲突，优先级如下：

1. 业务信息架构
2. 模块密度约束
3. 视觉风格
4. 局部组件样式

---

## 开放日模块设计系统

## 流程结构

### 用户流程
1. **第一步** - 上传页：只做数据上传和字段映射
2. **第二步** - 测算页：参数调整 + 结果表格

> 设计原则：一次只做一件事，认知负荷最小。数据源在第一步做完，第二步不要再重复。

### 测算页布局（最终版）
**上下结构**（而非左右结构），符合从上到下浏览习惯：

```
┌─────────────────────────────────────────────────────┐
│ 顶部导航：返回上传 + 文件名 + [更换文件] [测算]       │
├─────────────────────────────────────────────────────┤
│  参数卡片（一个整体卡片）                            │
│  ┌─────────────┐                                    │
│  │ 策略选择    │ 两列网格放预设                         │
│  └─────────────┘                                    │
│  ────────────────────────────────────────────────── 分界线 │
│  ┌──────┬──────┬──────┐                              │
│  │模式 │Alpha│商品权重│                              │
│  └──────┴──────┴──────┘                              │
│  ┌──────┐                                          │
│  │互动权重│  (3列网格自动换行到第二行)               │
│  └──────┘                                          │
│  ────────────────────────────────────────────────── 分界线 │
│  ┌──────┬──────┬──────┐                              │
│  │最低在售│最低好房│最低成交│                       │
│  └──────┴──────┴──────┘  (正好填满3列)              │
│  ────────────────────────────────────────────────── 分界线 │
│  ┌────┬────┬────┬────┐                              │
│  │规模│流量│商品│互动│  (正好填满4列)              │
│  └────┴────┴────┴────┘                              │
│  ──────────────────────────────────────────────────────────── │
│  [恢复默认] 按钮右对齐                                │
├─────────────────────────────────────────────────────┤
│  结果卡片（一个整体卡片）                            │
│  ┌─────────────────────────────────────────────────┐ │
│  │ 结果总览：样本/入围/冠军/缓存                      │ │
│  └─────────────────────────────────────────────────┘ │
│  ┌─────────────────────────────────────────────────┐ │
│  │ 完整排名表格                                      │ │
│  └─────────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────┘
```

## 颜色方案

| Role | Color | Usage |
|------|-------|-------|
| Background | `linear-gradient(#f8f5f1 0%, #f2ede6 100%)` | 页面背景 |
| Card Background | `rgba(255, 255, 255, 0.88)` | 卡片容器 |
| Border | `rgba(76, 58, 42, 0.12)` | 边框 |
| Border Divider | `rgba(76, 58, 42, 0.08)` | 分组分隔线 |
| Text Primary | `#1f1b17` | 标题、正文 |
| Text Secondary | `#4a433a` | Label 标签 |
| Primary Button | `#bf623b` (棕色) | 主按钮（下一步、测算） |
| Success | `#255648` | 成功提示文字 |
| Error | `#a8422e` | 错误提示文字 |
| Shadow | `rgba(69, 45, 20, 0.06)` | 卡片阴影 |

## 圆角

| Size | Usage |
|------|-------|
| 999px | 按钮、标签、进度条 |
| 18px | 表格、统计卡片 |
| 20px | 主卡片容器 |

## 间距尺度

| Size | Usage |
|------|-------|
| 4px | 文字间距 |
| 8px | 最小网格间距、按钮内间距 |
| 10px | 参数网格间距 |
| 12px | 次级间距 |
| 16px | 区块内间距 |
| 18px | 主卡片内间距、页面区块间距 |
| 20px | 策略网格间距 |
| 24px | 参数分组间距 |

## 字体

| Size | Line Height | Weight | Usage |
|------|------------|--------|-------|
| 28px | 1.2 | 700 | 页面大标题 |
| 20px | 1.3 | 700 | 区块标题 |
| 14px | 1.5 | 400 | 正文、按钮 |
| 13px | 1.5 | 400 | label、辅助文字 |
| 12px | 1.5 | 400 | 小标签、说明 |

**Font Family**：系统默认 Inter/sans-serif

## 布局网格

| Section | Grid | Description |
|---------|------|-------------|
| 策略选择 | `grid-template-columns: repeat(2, 1fr)` | 预设打法两列排列 |
| 基础参数 | `grid-template-columns: repeat(3, 1fr)` | 4个参数，自动换行 |
| 红线过滤 | `grid-template-columns: repeat(3, 1fr)` | 3个参数，正好填满 |
| 水位线设置 | `grid-template-columns: repeat(4, 1fr)` | 4个水位线，正好填满 |

## 分组原则

1. **同一个逻辑分组放在同一个网格**
2. **不同逻辑分组用分隔线分开**
3. **参数数量匹配网格列数**，保证视觉填满
4. **所有参数放在一个大卡片**，减少视觉碎片

## 卡片设计

| 之前 | 现在 |
|------|------|
| 每个模块一个独立卡片 | 同类模块放同一个大卡片 |
| 多个边框多个阴影 → 视觉碎 | 一个边框一个阴影 → 整体干净 |
| 左右布局 → 空间紧张 | 上下布局 → 空间充裕 |

## 断点响应式

| Breakpoint | Layout |
|------------|--------|
| `>= 1080px` | 保持设计网格 |
| `< 1080px` | 所有网格变成 1fr 单列 |

## 动画

- 所有交互 hover 向上 1px
- transition: 160ms ease

## 设计原则

1. **少即是多**：删掉所有不必要的说明文字，只保留操作控件
2. **分组清晰**：逻辑相近的参数放一起，用分隔线区分不同组
3. **视觉统一**：一个逻辑区域一个卡片，不要碎开
4. **流向自然**：参数从上到下 → 结果从上到下，符合阅读习惯
5. **足够留白**：底部保留足够间距，确保表格完整可见
6. **参数对齐**：根据参数数量选择网格列数，尽量填满避免缺口
